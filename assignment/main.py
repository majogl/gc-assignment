from wien_bikes.importer import Importer

# Pretty print of the sorted and filtered list of stations with added address
# To output a list of dicts, in the same order, use i.get_stations() 
i = Importer()
print(i)
